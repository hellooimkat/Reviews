config:
  target: 'http://127.0.0.1:3004'
  phases:
    - duration: 60
      arrivalRate: 100
scenarios:
  - flow:
    - get:
        url: "/api/get/reviews/overview/{{$randomNumber(1, 10000000)}}"

        
